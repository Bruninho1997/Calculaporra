<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bruno.calculaporra">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CalculadoraActivity" />
        <activity android:name=".metricaLOC" />
        <activity android:name=".RaizActivity" />
        <activity android:name=".ConfigActivity" />
        <activity android:name=".FatorialActivity" />
        <activity android:name=".PorcentagemActivity" />
        <activity android:name=".velocidadeMediaActivity" />
        <activity
            android:name=".sobreAtualizacoesActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MenuActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".ImcActivity" />
        <activity android:name=".PotencializacaoActicity"></activity>
    </application>

</manifest>